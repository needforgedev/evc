name: evc_core
description: "EVC shared core — data models, Supabase client, auth, config, DI (Riverpod)."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

# Part of the EVC pub workspace (see repo-root pubspec.yaml).
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  # riverpod, freezed, etc. get added here as Phase 0 progresses.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
